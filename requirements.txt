# Customer Intelligence Studio - runtime dependencies
# Deployable as-is on Streamlit Community Cloud (Python 3.10+).
streamlit>=1.36,<2
pandas>=2.0,<3
numpy>=1.24,<3
duckdb>=1.0,<2
plotly>=5.20,<7
scikit-learn>=1.3,<2
python-dotenv>=1.0,<2

# Optional: only used when OPENAI_API_KEY is configured.
# Every feature of the application works without it.
openai>=1.30,<3
